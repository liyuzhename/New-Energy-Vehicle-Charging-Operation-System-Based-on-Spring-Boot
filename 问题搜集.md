#### [已修复]
发起充电时报错
Uncaught (in promise) Error: 系统内部错误，请稍后重试
    at request.interceptors.response.use.status (request.js:29:29)
    at async Axios.request (Axios.js:41:14)
    at async handleStart (ChargingStart.vue:164:17)
    at Axios.request (Axios.js:46:41)
    at async handleStart (ChargingStart.vue:164:17)

start
{code: 500, message: "系统内部错误，请稍后重试", data: null}
code: 500
data: null
message: "系统内部错误，请稍后重试"

2026-06-11T12:26:25.470+08:00 ERROR 25112 --- [nio-8090-exec-9] c.c.c.exception.GlobalExceptionHandler   : 绯荤粺寮傚父: 

org.springframework.http.converter.HttpMessageNotReadableException: JSON parse error: Unrecognized field "gunId" (class com.charging.dto.StartChargingRequest), not marked as ignorable
----

##
注意事项
1.这是前后端分离的项目，现在处于后端，只需要修改每个问题中后端的部分即可。
2.每个问题中后端的部分修改完成后都要对应的把前端需要修改的部分写入到一个新的markdown文档中，用来修改前端，如果前端不需要修改请提示。
3.如果该问题需要修改数据库，请运行sql文件进行修改。
4.每个问题完成后，后端都要进行相应的curl测试，测试通过后进行git提交。
5.有的问题也有可能是前端的字段和后端的字段不一致导致的，如果判断出是这种问题，请将前端需要的字段提供给前端，同样是写入到那个新的markdown文档中

--

##
具体流程
修改后端问题➡进行curl测试➡前端问题写入markdown文档➡提交git
以上环节中如果某个环节没有错误就跳过