####
管理员端进入仪表盘时出现以下错误
Uncaught (in promise) Error: 系统内部错误，请稍后重试
    at request.interceptors.response.use.status (request.js:29:29)
    at async Axios.request (axios.js?v=9b8e9d38:2324:14)
    at async loadStats (AdminDashboard.vue:80:17)
    at Axios.request (axios.js?v=9b8e9d38:2328:41)
    at async loadStats (AdminDashboard.vue:80:17)

----

####
管理员端的用户管理界面中的按状态搜索功能无效

----

####
管理员端的订单管理界面中的用户、充电站、时长、金额都为空

----

####
管理员端的故障管理界面的充电桩、运营商、提报人字段为空，并且该界面的按充电站搜索功能也无效

----

####
管理员端的评价管理界面中的用户、充电站、评分都为空，并且该界面的按充电站、状态搜索功能也无效

----

####
管理员端的公告管理界面的按标题、类型、状态搜索全都无效，并且没有搜索按钮

----

####
管理员端的统计报表中的数据为空

----

####
管理员端的操作日志界面中的操作人、角色、操作内容、操作模块为空

----

##
注意事项
1.这是前后端分离的项目，现在处于后端，只需要修改每个问题中后端的部分即可。
2.每个问题中后端的部分修改完成后都要对应的把前端需要修改的部分写入到一个新的markdown文档中，用来修改前端，如果前端不需要修改请提示。
3.如果该问题需要修改数据库，请运行sql文件进行修改。
4.每个问题完成后，后端都要进行相应的curl测试，测试通过后进行git提交。
5.有的问题也有可能是前端的字段和后端的字段不一致导致的，如果判断出是这种问题，请将前端需要的字段提供给前端，同样是写入到那个新的markdown文档中

--

##
具体流程
修改后端问题➡进行curl测试➡前端问题写入markdown文档➡提交git
以上环节中如果某个环节没有错误就跳过